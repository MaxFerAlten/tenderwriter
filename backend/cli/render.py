"""Minimal renderer for CLI outputs (Wave 1)."""

from __future__ import annotations

def render(text: str) -> str:
    # Simple passthrough; in future this could format outputs for a CLI
    return text

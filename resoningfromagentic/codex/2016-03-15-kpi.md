procedi con lo sprint 12


Durata lavoro: 30m 50s
Sprint 12 e' completato, testato, committato e pushato su origin/codex/feature/kpi-reason-engine con commit e1adaea (feat(kpi): add forecast and history replay). La working tree ora e' pulita.

Abbiamo chiuso i blocchi previsti:

forecast rule-based e history/backfill nel motore KPI in main.py, forecasting.py, schemas.py, store.py e job_worker.py
endpoint admin BFF per history backfill e fallback aggiornati in kpi_admin.py
contratti frontend, dashboard admin, timeline storica e forecast explainable in client.ts, ObservabilityKPI.tsx e TransitionTimelinePanel.tsx
copertura test nuova in test_api.py, test_kpi_admin_api.py e client.test.ts
Verifiche eseguite prima del commit:

compileall su kpi-reason-engine, backend e test: OK
kpi-reason-engine tests: 18 OK
backend KPI tests: 18 OK
backend/verify_imports.py: OK
frontend tests: 18 OK
frontend build: OK
Il prossimo sprint naturale adesso e' Sprint 13, cioe' release readiness: golden dataset, end-to-end KPI, monitoring/alerting e runbook operativo.


1 file modificato
Annulla
kpi-reason-engine/app/main.py
si procedi con il 13


Durata lavoro: 2h 4m 30s
Sprint 13 e' chiuso su codex/feature/kpi-reason-engine con commit 9494c3a (feat(kpi): add release readiness hardening) ed e' gia' pushato su origin. Il branch e' pulito.

Abbiamo aggiunto il pacchetto di release readiness vero:

metriche runtime del servizio KPI in metrics.py ed esposizione GET /metrics in main.py
metriche persistite lato store in store.py
hardening del calcolo health per gate falliti in analytics.py
audit logging admin e fallback degradato piu' esplicito in kpi_admin.py
golden dataset e regression suite in golden_dataset.py, test_golden_dataset.py e test_runtime_metrics.py
documentazione operativa in runbook.md, monitoring-and-alerting.md, release-checklist.md e golden-dataset.md
Verifiche eseguite prima del commit:

kpi-reason-engine: 20 test OK
backend KPI suites: 19 test OK
backend/verify_imports.py: OK
frontend: 18 test OK
frontend build: OK
compileall: OK
Nota utile: durante i test e' emerso un unico mismatch nel golden dataset compliance_risk; l’ho corretto rendendo compliance_gate_failed un driver esplicito di health = red, che era la semantica giusta del caso. Se vuoi, il prossimo passo naturale e' fare il punto finale su quanto siamo vicini al 10/10 e aprire la PR.


1 file modificato
Annulla
kpi-reason-engine/app/analytics.py
si fai il punto

Stato attuale

Sul piano degli sprint, siamo arrivati in fondo: Sprint 1 -> Sprint 13 e' implementato sul branch codex/feature/kpi-reason-engine, ultimo commit 9494c3a.

A oggi il modulo esiste davvero end-to-end:

servizio separato tw-kpi-reason-engine
integrazione tw-backend -> tw-kpi-reason-engine
pagina admin observability-kpi
KPI A1..A4, B1..B4, Q, E
stato analitico, transizioni, forecast iniziale, replay/backfill
migrazioni, test, metriche runtime, audit logging, runbook e checklist release
Quanto siamo vicini al 10/10

La mia valutazione onesta oggi e':

PDF / aderenza funzionale: 9.5/10
codebase / implementazione tecnica: 9/10
production readiness reale: 8.5/10
Il motivo per cui non dico ancora 10/10 pieno e' che il pezzo di codice e' molto avanti, ma il vero 10/10 richiede anche chiusure operative fuori dal solo repository.

Cosa e' chiuso bene

Architettura separata: si'
BFF via tw-backend: si'
Dashboard admin osservability: si'
Telemetria operativa: si'
Explainability/versioning KPI: si'
Forecast rule-based: si'
Backfill/replay: si'
Golden dataset e regression suite: si'
Metriche /metrics, audit log e documentazione operativa: si'
Riferimenti chiave:

main.py
analytics.py
kpi_admin.py
ObservabilityKPI.tsx
runbook.md
release-checklist.md
Cosa manca per il vero 10/10

integrazione dell’osservabilita' in ambiente reale: dashboard/alert veri collegati all’infrastruttura
prove di carico e soak test
security review piu' formale su auth interna, secrets e superfici admin
validazione UAT su tender reali e calibrazione finale delle soglie KPI/forecast
eventuale separazione ancora piu' forte del worker/queue se vuoi hardening distribuito completo
procedure operative reali di backup/restore e retention dati in ambiente di deploy
Verdetto

Nel repository siamo messi molto bene: per me questo e' un modulo release candidate serio, non piu' un prototipo.
Se domani dovessimo aprire PR e andare verso staging, io lo considererei pronto. Per produzione piena enterprise, farei ancora un ultimo blocco breve di hardening operativo.

Se vuoi, il prossimo passo migliore e' uno di questi due:

preparo una PR summary completa e pulita;
ti faccio una gap list finale verso 10/10 con task residui, in ordine di priorita'.

Allegato dell'utente
trova il motivo


Durata lavoro: 2m 56s
Il motivo era questo: la route esisteva in observability.py, ma il router observability non veniva registrato nell’app FastAPI in main.py.

Per questo succedeva esattamente il comportamento che vedi:

snapshot, diagnostics, forecast rispondevano
workspace e le altre route /api/tenders/{id}/observability/... andavano in 404
Ho gia' corretto il problema aggiungendo app.include_router(observability.router, prefix="/api/tenders", ...) in main.py, e ho aggiunto anche un test di regressione in test_main_route_registration.py.

Verifiche fatte:

compileall backend/app backend/tests
test route registration: OK
backend/verify_imports.py: OK
Ho anche commitato e pushato il fix:

3ddab85 fix(backend): register observability routes
Se il backend che stai guardando su localhost:3000 punta a un server gia' avviato senza reload, fai un restart del backend e il 404 dovrebbe sparire.


Allegato dell'utente
Service degraded



Durata lavoro: 7m 14s
Il motivo era operativo, non nel frontend.

tw-kpi-reason-engine era unhealthy in Docker e nei log c'era questo errore:

ModuleNotFoundError: No module named 'alembic'
Quindi la pagina andava in Service degraded perche' il backend non riusciva a parlare con il motore KPI. Il punto importante e' questo:

nel repo alembic e' gia' dichiarato in pyproject.toml
il problema era il container vecchio, non ricreato con l’immagine aggiornata
Ho fatto:

rebuild del servizio kpi-reason-engine
force-recreate del container
verifica health su http://localhost:8010/health
Adesso lo stato e':

container tw-kpi-reason-engine: healthy
health endpoint: {"status":"healthy","service":"tw-kpi-reason-engine","version":"0.1.0"}
In pratica, il degrado era causato da un container KPI stantio/malato.
Ora fai un refresh della pagina Observability KPI.

Nota: se dopo il refresh vedi ancora card vuote o 0 tracked tenders, quello non sarebbe piu' un problema di servizio giu', ma di mirror KPI rimasto vuoto mentre il motore era offline. In quel caso il prossimo fix utile e' rendere Recompute KPI capace di resyncare anche il tender, non solo lanciare il job.


in ogni caso è meglio renderlo capace di resyncare


Durata lavoro: 3m 12s
Ho reso Recompute KPI e Replay History capaci di resyncare davvero il tender prima di lanciare il job.

La modifica e' in kpi_admin.py: ora il flusso admin fa publish_tender_sync(...) sul motore KPI e solo se il sync va a buon fine parte con full_recompute o history_backfill. Se il resync fallisce, il backend risponde subito con errore invece di mettere in coda un job inutile. Ho aggiunto anche la regressione in test_kpi_admin_api.py.

Verifiche eseguite:

compileall backend/app backend/tests
backend KPI suites: 20 test OK
backend/verify_imports.py: OK
Commit e push:

17e0313 fix(kpi): resync tender before admin analysis jobs
In piu', il servizio tw-kpi-reason-engine ora e' tornato healthy, quindi adesso il comportamento corretto e':

apri Observability KPI
selezioni un tender
premi Recompute KPI
il backend lo risincronizza nel motore e poi avvia l’analisi
Se vuoi, il prossimo passo lo faccio io: controllo se conviene aggiungere anche un’azione admin “Resync all mirrored tenders” per ripopolare rapidamente il portfolio dopo un outage.


si conviene aggiungi


Aggiungo una vera azione admin di recupero portfolio: endpoint backend per resync di tutti i tender verso il motore KPI, UI dedicata in Observability KPI, e test per evitare regressioni dopo outage del servizio.

Esecuzione completata di & 'C:\Program Files\PowerShell\7\pwsh.exe' -Command "Get-Content 'D:\tender\tenderwriter\backend\app\api\kpi_admin.py'"


Esecuzione completata di & 'C:\Program Files\PowerShell\7\pwsh.exe' -Command "Get-Content 'D:\tender\tenderwriter\frontend\src\api\client.ts'"


Esecuzione completata di & 'C:\Program Files\PowerShell\7\pwsh.exe' -Command "Get-Content 'D:\tender\tenderwriter\frontend\src\pages\ObservabilityKPI.tsx'"


Sto completando il pezzo “operativo”: endpoint portfolio-wide nel backend e bottone in pagina. Prima guardo il pattern dei test frontend cosi' lo inseriamo nello stesso stile del repo.

Esecuzione completata di & 'C:\Program Files\PowerShell\7\pwsh.exe' -Command "Get-Content 'D:\tender\tenderwriter\frontend\src\api\client.test.ts'"


Esecuzione completata di & 'C:\Program Files\PowerShell\7\pwsh.exe' -Command "Get-Content 'D:\tender\tenderwriter\backend\app\models\__init__.py' | Select-String -Pattern 'class Tender\(|Tender =' -Context 0,20"


Parto dal backend: se il bulk resync non e' robusto e non continua sugli errori parziali, in admin rischia di essere poco utile. Lo faccio resiliente e con un summary chiaro dei tender sincronizzati e falliti.

Esecuzione completata di @' from pathlib import Path path = Path(r'D:\tender\tenderwriter\backend\app\api\kpi_admin.py') text = path.read_text() text = text.replace( 'from fastapi import APIRouter, Depends, HTTPException, status\nfrom sqlalchemy.ext.asyncio import AsyncSession\n\nfrom app.api.auth import UserResponse, get_current_user\nfrom app.db.database import get_db\nfrom app.models import KpiEventDeliveryStatus\n', 'from fastapi import APIRouter, Depends, HTTPException, status\nfrom sqlalchemy import select\nfrom sqlalchemy.ext.asyncio import AsyncSession\n\nfrom app.api.auth import UserResponse, get_current_user\nfrom app.db.database import get_db\nfrom app.models import KpiEventDeliveryStatus, Tender\n' ) old = '''async def _sync_tender_before_analysis_job( *, tender_id: int, current_user: UserResponse, db: AsyncSession, client: KpiReasonEngineClient, ) -> KpiClientResult: sync_event = await publish_tender_sync( db, tender_id=tender_id, actor_id=current_user.id, source="tw-backend", client=client, ) if sync_event is None: raise HTTPException( status_code=status.HTTP_404_NOT_FOUND, detail=f"Tender {tender_id} not found.", ) delivery_status = getattr(sync_event.delivery_status, "value", sync_event.delivery_status) return KpiClientResult( delivered=delivery_status == KpiEventDeliveryStatus.DELIVERED.value, status_code=sync_event.response_status_code, response_json=dict(sync_event.response_json or {}), error_message=sync_event.error_message, ) ''' new = '''async def _sync_tender_before_analysis_job( *, tender_id: int, current_user: UserResponse, db: AsyncSession, client: KpiReasonEngineClient, ) -> KpiClientResult: sync_event = await publish_tender_sync( db, tender_id=tender_id, actor_id=current_user.id, source="tw-backend", client=client, ) if sync_event is None: raise HTTPException( status_code=status.HTTP_404_NOT_FOUND, detail=f"Tender {tender_id} not found.", ) delivery_status = getattr(sync_event.delivery_status, "value", sync_event.delivery_status) return KpiClientResult( delivered=delivery_status == KpiEventDeliveryStatus.DELIVERED.value, status_code=sync_event.response_status_code, response_json=dict(sync_event.response_json or {}), error_message=sync_event.error_message, ) async def _load_portfolio_tender_ids(db: AsyncSession) -> list[int]: result = await db.execute(select(Tender.id).order_by(Tender.id.asc())) return list(result.scalars().all()) ''' if old not in text: raise SystemExit('Target sync helper block not found in kpi_admin.py') text = text.replace(old, new) insert_after = '''@router.get("/portfolio/bottlenecks", response_model=dict[str, Any]) async def get_kpi_portfolio_bottlenecks( current_user: UserResponse = Depends(get_current_user), ) -> dict[str, Any]: _require_admin(current_user) client = KpiReasonEngineClient() result = await client.get_portfolio_bottlenecks() _audit_admin_event(action="portfolio_bottlenecks_query", current_user=current_user, result=result) return _query_or_fallback( result, action="portfolio bottlenecks query", fallback=_bottlenecks_fallback(_error_detail(result, "portfolio bottlenecks query")), ) ''' new_block = '''@router.get("/portfolio/bottlenecks", response_model=dict[str, Any]) async def get_kpi_portfolio_bottlenecks( current_user: UserResponse = Depends(get_current_user), ) -> dict[str, Any]: _require_admin(current_user) client = KpiReasonEngineClient() result = await client.get_portfolio_bottlenecks() _audit_admin_event(action="portfolio_bottlenecks_query", current_user=current_user, result=result) return _query_or_fallback( result, action="portfolio bottlenecks query", fallback=_bottlenecks_fallback(_error_detail(result, "portfolio bottlenecks query")), ) @router.post("/portfolio/resync", response_model=dict[str, Any]) async def resync_kpi_portfolio( current_user: UserResponse = Depends(get_current_user), db: AsyncSession = Depends(get_db), ) -> dict[str, Any]: _require_admin(current_user) client = KpiReasonEngineClient() tender_ids = await _load_portfolio_tender_ids(db) items: list[dict[str, Any]] = [] synced_tenders = 0 failed_tenders = 0 for tender_id in tender_ids: try: sync_result = await _sync_tender_before_analysis_job( tender_id=tender_id, current_user=current_user, db=db, client=client, ) except HTTPException as exc: sync_result = KpiClientResult( delivered=False, status_code=exc.status_code, response_json={}, error_message=str(exc.detail), ) except Exception as exc: sync_result = KpiClientResult( delivered=False, status_code=None, response_json={}, error_message=str(exc), ) _audit_admin_event(action="portfolio_tender_resync", current_user=current_user, tender_id=tender_id, result=sync_result) items.append({"tender_id": tender_id, **_sync_result_metadata(sync_result)}) if sync_result.delivered: synced_tenders += 1 else: failed_tenders += 1 return { "status": "completed", "generated_at": datetime.now(timezone.utc).isoformat(), "total_tenders": len(tender_ids), "synced_tenders": synced_tenders, "failed_tenders": failed_tenders, "items": items, "notes": [ "Portfolio resync completed successfully." if failed_tenders == 0 else "Portfolio resync completed with partial failures.", ], } ''' if insert_after not in text: raise SystemExit('Portfolio bottlenecks block not found for resync insertion') text = text.replace(insert_after, new_block) path.write_text(text) '@ | .\venv\Scripts\python -


Sto aggiungendo la regressione backend per il bulk resync: deve resyncare tutti i tender, contare successi/fallimenti e non fermarsi al primo errore.

Esecuzione completata di @' from pathlib import Path path = Path(r'D:\tender\tenderwriter\backend\tests\test_kpi_admin_api.py') text = path.read_text() insert_after = ''' def test_snapshot_query_falls_back_when_kpi_service_is_unavailable(self) -> None: mock_client = _MockKpiClient( snapshot_result=KpiClientResult( delivered=False, status_code=None, response_json={}, error_message="KPI service timeout", ) ) with patch("app.api.kpi_admin.KpiReasonEngineClient", return_value=mock_client): response = self.client.get("/admin/kpi/tenders/12/snapshot") self.assertEqual(response.status_code, 200) payload = response.json() self.assertEqual(payload["external_tender_id"], "12") self.assertEqual(payload["health"], "unknown") self.assertEqual(payload["notes"], ["KPI service timeout"]) self.assertTrue(payload["degraded"]) self.assertEqual(payload["degraded_reason"], "KPI service timeout") ''' new_block = ''' def test_snapshot_query_falls_back_when_kpi_service_is_unavailable(self) -> None: mock_client = _MockKpiClient( snapshot_result=KpiClientResult( delivered=False, status_code=None, response_json={}, error_message="KPI service timeout", ) ) with patch("app.api.kpi_admin.KpiReasonEngineClient", return_value=mock_client): response = self.client.get("/admin/kpi/tenders/12/snapshot") self.assertEqual(response.status_code, 200) payload = response.json() self.assertEqual(payload["external_tender_id"], "12") self.assertEqual(payload["health"], "unknown") self.assertEqual(payload["notes"], ["KPI service timeout"]) self.assertTrue(payload["degraded"]) self.assertEqual(payload["degraded_reason"], "KPI service timeout") def test_portfolio_resync_reports_successes_and_failures(self) -> None: mock_client = _MockKpiClient() load_ids_mock = AsyncMock(return_value=[12, 18]) sync_mock = AsyncMock(side_effect=[ KpiClientResult(True, 202, {"external_tender_id": "12"}), KpiClientResult(False, 502, {}, "Upstream timeout"), ]) with patch("app.api.kpi_admin.KpiReasonEngineClient", return_value=mock_client), patch("app.api.kpi_admin._load_portfolio_tender_ids", load_ids_mock), patch("app.api.kpi_admin._sync_tender_before_analysis_job", sync_mock): response = self.client.post("/admin/kpi/portfolio/resync") self.assertEqual(response.status_code, 200) payload = response.json() self.assertEqual(payload["total_tenders"], 2) self.assertEqual(payload["synced_tenders"], 1) self.assertEqual(payload["failed_tenders"], 1) self.assertEqual(payload["items"][0]["tender_id"], 12) self.assertTrue(payload["items"][0]["delivered"]) self.assertEqual(payload["items"][1]["tender_id"], 18) self.assertEqual(payload["items"][1]["error_message"], "Upstream timeout") load_ids_mock.assert_awaited_once() self.assertEqual(sync_mock.await_count, 2) ''' if insert_after not in text: raise SystemExit('Snapshot block not found for portfolio resync test insertion') text = text.replace(insert_after, new_block) path.write_text(text) '@ | .\venv\Scripts\python -


Passo al client frontend: aggiungo il contratto typed del resync portfolio e il metodo API, cosi' la pagina admin puo' usarlo senza chiamate “raw”.

Esecuzione completata di @' from pathlib import Path path = Path(r'D:\tender\tenderwriter\frontend\src\api\client.ts') text = path.read_text() text = text.replace( ' getPortfolioOverview: () => request<KpiPortfolioOverview>(\'/admin/kpi/portfolio/overview\'),\n getPortfolioBottlenecks: () => request<KpiPortfolioBottlenecks>(\'/admin/kpi/portfolio/bottlenecks\'),\n', ' getPortfolioOverview: () => request<KpiPortfolioOverview>(\'/admin/kpi/portfolio/overview\'),\n getPortfolioBottlenecks: () => request<KpiPortfolioBottlenecks>(\'/admin/kpi/portfolio/bottlenecks\'),\n resyncPortfolio: () => request<KpiPortfolioResyncResult>(\'/admin/kpi/portfolio/resync\', { method: \'POST\' }),\n' ) insert_after = '''export interface KpiPortfolioBottlenecks { status: string; generated_at: string | null; items: KpiBottleneckItem[]; } ''' new_block = '''export interface KpiPortfolioBottlenecks { status: string; generated_at: string | null; items: KpiBottleneckItem[]; } export interface KpiPortfolioResyncItem { tender_id: number; delivered: boolean; upstream_status_code: number | null; error_message: string | null; } export interface KpiPortfolioResyncResult { status: string; generated_at: string | null; total_tenders: number; synced_tenders: number; failed_tenders: number; items: KpiPortfolioResyncItem[]; notes: string[]; } ''' if insert_after not in text: raise SystemExit('KpiPortfolioBottlenecks block not found in client.ts') text = text.replace(insert_after, new_block) path.write_text(text) '@ | .\venv\Scripts\python -


Aggiungo anche il test frontend del client: deve chiamare il nuovo endpoint admin corretto e restituire il summary typed del resync.

Esecuzione completata di @' from pathlib import Path path = Path(r'D:\tender\tenderwriter\frontend\src\api\client.test.ts') text = path.read_text() insert_after = ''' it('queries portfolio overview through the admin KPI BFF', async () => { localStorageMock.setItem('token', 'admin-token'); fetchMock.mockResolvedValue( new Response(JSON.stringify({ total_tenders: 3, status: 'not_ready' }), { status: 200, headers: { 'Content-Type': 'application/json' }, }) ); const response = await kpiAdminApi.getPortfolioOverview(); expect(response.total_tenders).toBe(3); expect(fetchMock).toHaveBeenCalledWith( '/api/admin/kpi/portfolio/overview', expect.objectContaining({ method: 'GET', headers: expect.objectContaining({ Authorization: 'Bearer admin-token', 'Content-Type': 'application/json', }), }) ); }); ''' new_block = ''' it('queries portfolio overview through the admin KPI BFF', async () => { localStorageMock.setItem('token', 'admin-token'); fetchMock.mockResolvedValue( new Response(JSON.stringify({ total_tenders: 3, status: 'not_ready' }), { status: 200, headers: { 'Content-Type': 'application/json' }, }) ); const response = await kpiAdminApi.getPortfolioOverview(); expect(response.total_tenders).toBe(3); expect(fetchMock).toHaveBeenCalledWith( '/api/admin/kpi/portfolio/overview', expect.objectContaining({ method: 'GET', headers: expect.objectContaining({ Authorization: 'Bearer admin-token', 'Content-Type': 'application/json', }), }) ); }); it('triggers a portfolio resync through the admin KPI BFF', async () => { fetchMock.mockResolvedValue( new Response(JSON.stringify({ status: 'completed', total_tenders: 4, synced_tenders: 4, failed_tenders: 0, items: [], notes: [] }), { status: 200, headers: { 'Content-Type': 'application/json' }, }) ); const response = await kpiAdminApi.resyncPortfolio(); expect(response.total_tenders).toBe(4); expect(response.synced_tenders).toBe(4); expect(fetchMock).toHaveBeenCalledWith( '/api/admin/kpi/portfolio/resync', expect.objectContaining({ method: 'POST', }) ); }); ''' if insert_after not in text: raise SystemExit('Overview test block not found in client.test.ts') text = text.replace(insert_after, new_block) path.write_text(text) '@ | .\venv\Scripts\python -


Sto chiudendo il lato UX: bottone in alto, stato di loading dedicato e un messaggio chiaro di esito del resync portfolio, cosi' l’admin capisce subito cosa e' successo.

Esecuzione completata di @' from pathlib import Path path = Path(r'D:\tender\tenderwriter\frontend\src\pages\ObservabilityKPI.tsx') text = path.read_text() text = text.replace( ' const [isDetailLoading, setIsDetailLoading] = useState(false);\n const [isRecomputing, setIsRecomputing] = useState(false);\n const [error, setError] = useState<string | null>(null);\n', ' const [isDetailLoading, setIsDetailLoading] = useState(false);\n const [isRecomputing, setIsRecomputing] = useState(false);\n const [isPortfolioResyncing, setIsPortfolioResyncing] = useState(false);\n const [error, setError] = useState<string | null>(null);\n const [actionNotice, setActionNotice] = useState<string | null>(null);\n' ) text = text.replace( ' const recomputeDisabled = !selectedTenderId || isDetailLoading || isAnalysisJobActive(analysisJob);\n const backfillDisabled = recomputeDisabled;\n const activeJobType = analysisJob?.job_type || null;\n\n const handleRecompute = async () => {\n', ' const recomputeDisabled = !selectedTenderId || isDetailLoading || isAnalysisJobActive(analysisJob);\n const backfillDisabled = recomputeDisabled;\n const portfolioResyncDisabled = isPortfolioResyncing || isRefreshing || isLoading;\n const activeJobType = analysisJob?.job_type || null;\n\n const handlePortfolioResync = async () => {\n setError(null);\n setActionNotice(null);\n setIsPortfolioResyncing(true);\n try {\n const response = await kpiAdminApi.resyncPortfolio();\n const total = response.total_tenders ?? 0;\n const synced = response.synced_tenders ?? 0;\n const failed = response.failed_tenders ?? 0;\n setActionNotice(\n failed > 0\n ? `Portfolio resync completed: ${synced}/${total} synced, ${failed} failed.`\n : `Portfolio resync completed: ${synced}/${total} tenders synced into the KPI engine.`\n );\n await loadPortfolio(true);\n } catch (resyncError) {\n setError(resyncError instanceof Error ? resyncError.message : 'Failed to resync the KPI portfolio.');\n } finally {\n setIsPortfolioResyncing(false);\n }\n };\n\n const handleRecompute = async () => {\n' ) text = text.replace( ''' <button className={`btn btn-secondary btn-sm ${isRefreshing ? 'animate-pulse' : ''}`} onClick={() => void loadPortfolio(true)} > <RefreshCcw size={14} /> Refresh </button> <button ''', ''' <button className={`btn btn-secondary btn-sm ${isRefreshing ? 'animate-pulse' : ''}`} onClick={() => void loadPortfolio(true)} > <RefreshCcw size={14} /> Refresh </button> <button className={`btn btn-secondary btn-sm ${isPortfolioResyncing ? 'animate-pulse' : ''}`} onClick={() => void handlePortfolioResync()} disabled={portfolioResyncDisabled} > <RefreshCcw size={14} /> {isPortfolioResyncing ? 'Resyncing portfolio…' : 'Resync Portfolio'} </button> <button ''' ) text = text.replace( ''' {error && ( <div className="card" style={{ borderColor: 'rgba(239, 68, 68, 0.35)', background: 'rgba(127, 29, 29, 0.18)', marginBottom: '1.5rem' }}> <div style={{ display: 'flex', alignItems: 'center', gap: '0.75rem', color: '#fecaca' }}> <AlertTriangle size={18} /> <span>{error}</span> </div> </div> )} <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(220px, 1fr))', gap: '1rem', marginBottom: '1.5rem' }}> ''', ''' {error && ( <div className="card" style={{ borderColor: 'rgba(239, 68, 68, 0.35)', background: 'rgba(127, 29, 29, 0.18)', marginBottom: '1.5rem' }}> <div style={{ display: 'flex', alignItems: 'center', gap: '0.75rem', color: '#fecaca' }}> <AlertTriangle size={18} /> <span>{error}</span> </div> </div> )} {actionNotice && ( <div className="card" style={{ borderColor: 'rgba(16, 185, 129, 0.28)', background: 'rgba(6, 78, 59, 0.18)', marginBottom: '1.5rem' }}> <div style={{ display: 'flex', alignItems: 'center', gap: '0.75rem', color: '#bbf7d0' }}> <Activity size={18} /> <span>{actionNotice}</span> </div> </div> )} <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(220px, 1fr))', gap: '1rem', marginBottom: '1.5rem' }}> ''' ) path.write_text(text) '@ | .\venv\Scripts\python -


La prima patch del file pagina ha solo avuto un problema di quoting dentro lo script. La rifaccio in modo piu' robusto a blocchi, senza toccare la logica gia' scritta.

Esecuzione completata di @' from pathlib import Path path = Path(r'D:\tender\tenderwriter\frontend\src\pages\ObservabilityKPI.tsx') text = path.read_text() old = """ const [isDetailLoading, setIsDetailLoading] = useState(false); const [isRecomputing, setIsRecomputing] = useState(false); const [error, setError] = useState<string | null>(null); """ new = """ const [isDetailLoading, setIsDetailLoading] = useState(false); const [isRecomputing, setIsRecomputing] = useState(false); const [isPortfolioResyncing, setIsPortfolioResyncing] = useState(false); const [error, setError] = useState<string | null>(null); const [actionNotice, setActionNotice] = useState<string | null>(null); """ if old not in text: raise SystemExit('state block not found') text = text.replace(old, new) old = """ const recomputeDisabled = !selectedTenderId || isDetailLoading || isAnalysisJobActive(analysisJob); const backfillDisabled = recomputeDisabled; const activeJobType = analysisJob?.job_type || null; const handleRecompute = async () => { """ new = """ const recomputeDisabled = !selectedTenderId || isDetailLoading || isAnalysisJobActive(analysisJob); const backfillDisabled = recomputeDisabled; const portfolioResyncDisabled = isPortfolioResyncing || isRefreshing || isLoading; const activeJobType = analysisJob?.job_type || null; const handlePortfolioResync = async () => { setError(null); setActionNotice(null); setIsPortfolioResyncing(true); try { const response = await kpiAdminApi.resyncPortfolio(); const total = response.total_tenders ?? 0; const synced = response.synced_tenders ?? 0; const failed = response.failed_tenders ?? 0; setActionNotice( failed > 0 ? `Portfolio resync completed: ${synced}/${total} synced, ${failed} failed.` : `Portfolio resync completed: ${synced}/${total} tenders synced into the KPI engine.` ); await loadPortfolio(true); } catch (resyncError) { setError(resyncError instanceof Error ? resyncError.message : 'Failed to resync the KPI portfolio.'); } finally { setIsPortfolioResyncing(false); } }; const handleRecompute = async () => { """ if old not in text: raise SystemExit('action block not found') text = text.replace(old, new) old = """ <button className={`btn btn-secondary btn-sm ${isRefreshing ? 'animate-pulse' : ''}`} onClick={() => void loadPortfolio(true)} > <RefreshCcw size={14} /> Refresh </button> <button """ new = """ <button className={`btn btn-secondary btn-sm ${isRefreshing ? 'animate-pulse' : ''}`} onClick={() => void loadPortfolio(true)} > <RefreshCcw size={14} /> Refresh </button> <button className={`btn btn-secondary btn-sm ${isPortfolioResyncing ? 'animate-pulse' : ''}`} onClick={() => void handlePortfolioResync()} disabled={portfolioResyncDisabled} > <RefreshCcw size={14} /> {isPortfolioResyncing ? 'Resyncing portfolio…' : 'Resync Portfolio'} </button> <button """ if old not in text: raise SystemExit('button block not found') text = text.replace(old, new) old = """ {error && ( <div className="card" style={{ borderColor: 'rgba(239, 68, 68, 0.35)', background: 'rgba(127, 29, 29, 0.18)', marginBottom: '1.5rem' }}> <div style={{ display: 'flex', alignItems: 'center', gap: '0.75rem', color: '#fecaca' }}> <AlertTriangle size={18} /> <span>{error}</span> </div> </div> )} <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(220px, 1fr))', gap: '1rem', marginBottom: '1.5rem' }}> """ new = """ {error && ( <div className="card" style={{ borderColor: 'rgba(239, 68, 68, 0.35)', background: 'rgba(127, 29, 29, 0.18)', marginBottom: '1.5rem' }}> <div style={{ display: 'flex', alignItems: 'center', gap: '0.75rem', color: '#fecaca' }}> <AlertTriangle size={18} /> <span>{error}</span> </div> </div> )} {actionNotice && ( <div className="card" style={{ borderColor: 'rgba(16, 185, 129, 0.28)', background: 'rgba(6, 78, 59, 0.18)', marginBottom: '1.5rem' }}> <div style={{ display: 'flex', alignItems: 'center', gap: '0.75rem', color: '#bbf7d0' }}> <Activity size={18} /> <span>{actionNotice}</span> </div> </div> )} <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(220px, 1fr))', gap: '1rem', marginBottom: '1.5rem' }}> """ if old not in text: raise SystemExit('notice block not found') text = text.replace(old, new) path.write_text(text) '@ | .\venv\Scripts\python -


La feature e' cablata sia backend sia frontend. Ora faccio il giro completo di verifica: suite backend KPI, import check, test frontend e build, cosi' il bottone nuovo esce gia' certificato.

Esecuzione completata di & 'C:\Program Files\PowerShell\7\pwsh.exe' -Command ".\\venv\\Scripts\\python backend\\verify_imports.py"


Esecuzione completata di & 'C:\Program Files\PowerShell\7\pwsh.exe' -Command "Set-Location 'D:\tender\tenderwriter\backend'; ..\\venv\\Scripts\\python -m unittest discover -s tests -p 'test_kpi_*.py' -v"


Esecuzione completata di & 'C:\Program Files\PowerShell\7\pwsh.exe' -Command "Set-Location 'D:\tender\tenderwriter\frontend'; npm run test -- --run"


Esecuzione completata di & 'C:\Program Files\PowerShell\7\pwsh.exe' -Command "Set-Location 'D:\tender\tenderwriter\frontend'; npm run build"


Esecuzione completata di & 'C:\Program Files\PowerShell\7\pwsh.exe' -Command ".\\venv\\Scripts\\python -m compileall backend\\app backend\\tests"


Tutto e' verde anche lato frontend. Faccio l’ultimo passaggio di pulizia e poi committo/pusho il bulk resync, cosi' hai il recupero portfolio completo sul branch remoto.

Esecuzione completata di & 'C:\Program Files\PowerShell\7\pwsh.exe' -Command "git status --short"


Sto verificando solo un dettaglio di packaging: il build ha toccato index.html, e voglio includerlo solo se e' davvero un file tracciato del repo.

Esecuzione completata di & 'C:\Program Files\PowerShell\7\pwsh.exe' -Command "git ls-files frontend/dist/index.html"


Esecuzione completata di & 'C:\Program Files\PowerShell\7\pwsh.exe' -Command "git add backend/app/api/kpi_admin.py backend/tests/test_kpi_admin_api.py frontend/src/api/client.test.ts frontend/src/api/client.ts frontend/src/pages/ObservabilityKPI.tsx frontend/dist/index.html"


Il repo ignora frontend/dist, ma index.html e' tracciato. Lo aggiungo in modo esplicito con -f, come gia' fatto in questo progetto, e poi chiudo il commit.

Esecuzione completata di & 'C:\Program Files\PowerShell\7\pwsh.exe' -Command "git add backend/app/api/kpi_admin.py backend/tests/test_kpi_admin_api.py frontend/src/api/client.test.ts frontend/src/api/client.ts frontend/src/pages/ObservabilityKPI.tsx"


Esecuzione completata di & 'C:\Program Files\PowerShell\7\pwsh.exe' -Command "git add -f frontend/dist/index.html"


Esecuzione completata di & 'C:\Program Files\PowerShell\7\pwsh.exe' -Command "git commit -m 'feat(kpi): add portfolio resync action'"


Esecuzione completata di & 'C:\Program Files\PowerShell\7\pwsh.exe' -Command "git push -u origin codex/feature/kpi-reason-engine"


Esecuzione completata di & 'C:\Program Files\PowerShell\7\pwsh.exe' -Command "git status --short"


Durata lavoro: 5m 32s
Ho aggiunto la funzione che conviene davvero dopo un outage: Resync Portfolio.

Ora hai:

endpoint backend admin in kpi_admin.py che risincronizza tutti i tender presenti nel backend verso tw-kpi-reason-engine, continuando anche se alcuni falliscono;
bottone Resync Portfolio nella pagina ObservabilityKPI.tsx;
contratto client aggiornato in client.ts;
test backend e frontend aggiornati in test_kpi_admin_api.py e client.test.ts.
Verifiche fatte:

backend compile: OK
backend KPI suites: 21 test OK
backend/verify_imports.py: OK
frontend tests: 19 test OK
frontend build: OK
Commit e push:

3b74dd1 feat(kpi): add portfolio resync action
A questo punto il flusso giusto in UI e':

apri Observability KPI
clicca Resync Portfolio
poi usa Recompute KPI sul tender che vuoi analizzare più a fondo
Il branch codex/feature/kpi-reason-engine e' pulito e remoto aggiornato.

Mark received - fix finale

Durata lavoro: 16m circa
Problema osservato durante la simulazione browser su http://localhost:3000/observability-kpi:

il bottone Mark received era visibile ma il click normale falliva;
Playwright mostrava che il puntatore veniva intercettato dal layer della card Start review;
il backend non era il problema, perche' la POST di receive funzionava se forzata.

Diagnosi svolta:

simulazione reale in browser con login admin e flusso osservability-kpi;
creazione di un contribution di test e di una request di test sul tender hyper;
misurazione dei bounding box in pagina per capire il conflitto di layout;
verifica delle richieste di rete per distinguere problema UI da problema API;
individuazione della causa nel layout del workspace operativo: la colonna di destra poteva invadere quella di sinistra e coprire i controlli del Workflow stream.

Intervento fatto:

fix in frontend/src/components/observability/OperationalWorkspacePanel.tsx;
layout principale del workspace reso responsive con gridTemplateColumns: repeat(auto-fit, minmax(320px, 1fr));
aggiunto minWidth: 0 nei container interni critici per impedire overflow e overlap tra card;
aggiunte regressioni frontend in frontend/src/api/client.test.ts per i metodi usati nel flusso operativo:
- observabilityApi.receiveRequest(...)
- observabilityApi.createReview(...)

Verifiche eseguite:

frontend tests: 21 OK;
frontend build: OK;
rebuild del container frontend e restart del runtime locale;
nuova simulazione browser reale sulla stessa pagina.

Esito finale verificato in browser:

Mark received ora clicca normalmente senza force click;
la request passa correttamente a stato received;
il contribution passa a stato received;
la health del tender sale a green;
si aggiornano anche i KPI operativi:
- B1 = 100.0 green
- B2 = 100.0 green
- B4 = 100.0 green
- E = 100.0 green
il forecast si aggiorna verso submit on time con health green.

Commit del fix codice:

934c502 fix(frontend): unblock workspace receive action
